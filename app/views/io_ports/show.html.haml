- @title = @io_port.name.capitalize

%h2= @title
- if @io_port.hardware.empty?
  %p
    The port
    %strong= @io_port.name
    currently is not present in any computer or peripheral.
- else
  %table.list
    = render :partial => 'computers/hardware', :collection => @io_port.hardware