%h2= @co_cpu.name
- if @co_cpu.hardware.empty?
  %p
    The co-CPU
    %strong= @co_cpu.name
    is not currently used by any computer or peripheral in the database.
- else
  %table.hardware_list
    = render :partial => 'computers/hardware', :collection => @co_cpu.hardware