%p.breadcrumbs
  =link_to 'home', root_path
  » login

%h2 User Login
- form_for @user_session, :url => user_sessions_path do |f|
  %table.login
    %tr
      %th= f.label :email
      %td= f.text_field :email
    %tr
      %th= f.label :password
      %td= f.password_field :password
    %tr
      %td.center{:colspan => 2}= f.submit 'log in'

  = f.error_messages
