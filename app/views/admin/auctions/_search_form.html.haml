- form_tag admin_auctions_path, :method => :get do
  %p
    = label_tag :keywords
    = text_field_tag :keywords, params[:keywords]
    = label_tag :sort_by
    = select_tag :order, options_for_select(Auction::SEARCH_FIELDS, params[:order])
    = label_tag :desc
    = check_box_tag :desc, 'DESC', params[:desc]
    = hidden_field_tag :scope, params[:scope]
    = submit_tag 'search'