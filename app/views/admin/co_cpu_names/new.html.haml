%h1 New CPU Family

- form_for(@co_cpu_name, :url => admin_co_cpu_names_path) do |f|
  = f.error_messages
  %table.record
    %tr
      %th= f.label :name
      %td= f.text_field :name
    %tr
      %td.center{:colspan => 2}= f.submit 'Create'
  
%p= link_to 'Back', admin_co_cpu_names_path