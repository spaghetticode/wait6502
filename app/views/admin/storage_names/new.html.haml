%h1 New Storage Name

- form_for(@storage_name, :url => admin_storage_names_path) do |f|
  = f.error_messages
  %table.record
    %tr
      %th= f.label :name
      %td= f.text_field :name
    %tr
      %td.center{:colspan => 2}= f.submit 'Create'
      
%p= link_to 'Back', admin_storage_names_path