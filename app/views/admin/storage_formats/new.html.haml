%h1 New Storage Format

- form_for(@storage_format, :url => admin_storage_formats_path) do |f|
  = f.error_messages
  %table.record
    %tr
      %th= f.label :name
      %td= f.text_field :name
    %tr
      %td.center{:colspan => 2}= f.submit 'Create'

%p= link_to 'Back', admin_storage_formats_path