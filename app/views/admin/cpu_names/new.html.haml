%h1 New CPU Name

- form_for(@cpu_name, :url => admin_cpu_names_path) do |f|
  = f.error_messages
  %table.record
    %tr
      %th= f.label :name
      %td= f.text_field :name
    %tr
      %td.center{:colspan => 2}= f.submit 'Create'
  
%p= link_to 'Back', admin_cpu_names_path