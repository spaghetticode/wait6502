- @title = 'peripherals'

%h2 Peripherals
%table.list
  - @peripherals.each do |p|
    %tr
      %td= image_tag(p.images.first.filename(:thumb)) unless p.images.empty?
      %td= link_to p.name, peripheral_path(p)